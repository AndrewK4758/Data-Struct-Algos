{
  "name": "towers",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "towers/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/towers",
        "main": "towers/src/index.ts",
        "tsConfig": "towers/tsconfig.lib.json",
        "assets": ["towers/*.md"]
      }
    }
  }
}
